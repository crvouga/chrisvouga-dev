{
  "name": "chrisvouga.dev",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "bun --hot ./src/main.start.js",
    "build": "bun ./src/main.build.js",
    "preview": "bun run build && bun x serve dist",
    "tc": "bun x tsc -w"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.1.14"
  }
}
